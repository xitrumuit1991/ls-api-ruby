wb = xlsx_package.workbook
wb.add_worksheet(name: "Idol receive hearts") do |sheet|
	sheet.add_row ["Name", "Email", "Hearts"]
  @data.each do |item|
    sheet.add_row [item.room.broadcaster.fullname, item.room.broadcaster.user.email, item.quantity]
  end
end